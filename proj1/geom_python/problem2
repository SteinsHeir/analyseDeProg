Main {
Constraint EX : {
    x >= 2;
    y >= 1;
    x + 2y >= 6;
    x - 2y >= -6;
    y <= 1;
}

Constraint EX2 : {
    x >= 2;
    y >= 1;
    x <= 2;
    y <= 1;
}

Interval x of EX = [3, 10];
Interval y of EX = [0, 10];
}